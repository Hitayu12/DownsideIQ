# --- Core ---
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0
pyarrow>=14.0           # parquet I/O

# --- Market / price data ---
yfinance>=0.2.40        # primary MVP price source (no key required)

# --- ML / stats ---
scikit-learn>=1.3
xgboost>=2.0            # primary downside classifier
lightgbm>=4.0           # optional alternative classifier
statsmodels>=0.14       # quantile regression, baselines
arch>=6.3               # GARCH / EGARCH / GJR-GARCH

# --- News / external context (optional at runtime; pipeline degrades gracefully) ---
tavily-python>=0.3      # live news search
google-genai>=1.0        # Gemini structured news scoring (current SDK)
finnhub-python>=2.4     # company news / fundamentals / earnings calendar
requests>=2.31          # Alpha Vantage + generic HTTP

# --- Dashboard ---
streamlit>=1.30
plotly>=5.18

# --- Backend / services / infra (production) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
alembic>=1.13
celery>=5.3
redis>=5.0
structlog>=24.1
tenacity>=8.2
httpx>=0.27
typer>=0.12

# --- Testing ---
pytest>=7.4

# --- Optional (later phases) ---
# shap>=0.44            # SHAP explanations for the classifier
# fastapi>=0.110
# uvicorn>=0.27
